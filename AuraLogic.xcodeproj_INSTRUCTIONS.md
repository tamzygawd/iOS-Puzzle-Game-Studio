# Aura Logic Xcode Project

To open this project in Xcode:
1. Create a new "App" project in Xcode.
2. Name it "AuraLogic".
3. Drag the `Sources` and `Resources` folders into the Xcode project navigator.
4. Ensure `levels_v1.json` is added to the "AuraLogic" target.
5. Build and run!
